package layout

// SingleTabMode controls whether multi-tab features are active.
// v1: true (single-tab, tab bar hidden, tab keybindings disabled).
// Set to false to re-enable multi-tab in v2.
const SingleTabMode = true
