package extension

// Extension type identifiers.
const (
	extTypeLua  = "lua"
	extTypeMCP  = "mcp"
	extTypeWasm = "wasm"
)

// configFile is the expected manifest file inside an extension directory.
const configFile = "extension.toml"
