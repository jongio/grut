package main

// {{.Name}} is a tinygo-compatible WASM extension for grut.
//
// Build with: tinygo build -o extension.wasm -target wasi main.go

func main() {
	// Export functions that grut can call.
	// See grut extension documentation for the WASM API.
}
